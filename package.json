{
  "name": "p4-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run serve.js",
    "build": "bun run build-placeholder.js",
    "build:dev": "bun run build-placeholder.js",
    "dist-check": "bun run build-placeholder.js",
    "start": "bun run serve.js",
    "lint": "echo skipped",
    "test": "echo skipped",
    "prod:install": "cp package.production.json package.json && cp tsconfig.production.json tsconfig.json && pnpm install",
    "derive-creds": "node scripts/derive-clob-credentials.mjs"
  }
}
